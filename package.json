{
  "name": "twitter-follower-tracker",
  "version": "1.0.0",
  "description": "A Node.js app to track Twitter followers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "install": "npm install",
    "build": "npm install"  // 여기서 npm install을 명시적으로 실행
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.3.8"
  },
  "engines": {
    "node": "18.x"
  }
}
